<%= form_with(model: project_type_join, local: true) do |form| %>
  <% if project_type_join.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_type_join.errors.count, "error") %> prohibited this project_type_join from being saved:</h2>

      <ul>
      <% project_type_join.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :project_type_join_project_id %>
  </div>

  <div class="field">
    <%= form.label :project_type_id %>
    <%= form.text_field :project_type_id, id: :project_type_join_project_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
