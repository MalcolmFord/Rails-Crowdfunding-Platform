<%= form_with(model: payment_type, local: true) do |form| %>
  <% if payment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_type.errors.count, "error") %> prohibited this payment_type from being saved:</h2>

      <ul>
      <% payment_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :method %>
    <%= form.text_field :method, id: :payment_type_method %>
  </div>

  <div class="field">
    <%= form.label :card_number %>
    <%= form.number_field :card_number, id: :payment_type_card_number %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :payment_type_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
